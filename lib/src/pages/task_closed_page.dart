import 'package:flutter/material.dart';

class TaskClosedpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Hola putooo"),
      ),
    );
  }
}
